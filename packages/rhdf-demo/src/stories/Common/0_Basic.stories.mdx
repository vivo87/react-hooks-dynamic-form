import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import Form from "./Form";

<Meta title="Common/Basic Settings" component={Form} />

Check out all possible settings _markdown_!

# Basic Settings

<Preview>
  <Story name="Build-in type">
    <Form
      fields={[
        {
          name: "field-1",
          type: "email",
          label: "Email type auto validated",
          placeholder: "Input and see what happens",
        },
        {
          name: "phone",
          type: "phone",
          isRequired: true,
          placeholder: "Input and see what happens",
          label: "Phone type auto validated",
        },
      ]}
      defaultSettings={{
        validateOnChange: true,
      }}
    />
  </Story>
</Preview>

<!-- # Example 2

<Preview>
  <Story name="Example 2">
    <Form remoteData={{ login: "Jane", phone: "01-02-03-04-05" }} />
  </Story>
</Preview> -->

# Props

<Props of={Form} />
