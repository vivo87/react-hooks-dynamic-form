import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import Form from "./Form";

<Meta title="Common/Basic Settings" component={Form} />

# Default settings

We can provide a default common settings for all fields.

> Please note, these settings will be merged and eventually overridden by each specific field definition.

<Preview>
  <Story name="Default settings">
    <Form
      fields={[
        {
          name: "field-1",
          type: "text",
        },
        {
          name: "field-1",
          type: "text",
        },
      ]}
      defaultSettings={{
        value: "A common intial value for every field",
        validateOnChange: true,
      }}
    />
  </Story>
</Preview>

# Props

<Props of={Form} />
